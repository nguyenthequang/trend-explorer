name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      # Mapped to env because the `secrets` context is not allowed inside
      # `if:` expressions — testing env.* is GitHub's documented workaround.
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
    steps:
      - uses: actions/checkout@v4

      # Authenticated pulls dodge Docker Hub's anonymous rate limits
      # (Testcontainers pulls postgres:16). Pure optimization: continue-on-error
      # so a missing/invalid token never fails the build — it just falls back to
      # anonymous pulls.
      - name: Log in to Docker Hub
        if: ${{ github.event_name == 'push' && env.DOCKERHUB_USERNAME != '' }}
        continue-on-error: true
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build and test
        run: ./mvnw -q verify
