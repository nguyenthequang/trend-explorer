# Names only — real values live in your local env, GitHub Actions secrets,
# and Cloud Run env vars. Never commit real values.

# Neon Postgres (Phase 0b) — JDBC form: jdbc:postgresql://<host>/<db>?sslmode=require
DATABASE_URL=
DATABASE_USERNAME=
DATABASE_PASSWORD=

# Reddit script app (Phase 3)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USERNAME=

# LLM provider (Phase 5): gemini | groq
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=

# Budget/IP hashing salt (Phase 6)
IP_HASH_SALT=
